'use client';

// This component is temporarily disabled to prevent application crashes.
// The previous code contained a hydration error.
// We will restore and fix this component in a future step.

export function BookingFlow() {
  return (
    <div className="max-w-2xl mx-auto p-8 border rounded-lg">
      <h2 className="text-2xl font-bold text-center mb-4">Booking System Coming Soon</h2>
      <p className="text-center text-muted-foreground">
        Our interactive booking system is currently under maintenance. Please check back shortly!
      </p>
    </div>
  );
}
