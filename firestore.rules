
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Allow public read and write access to the appointments collection.
    // This is necessary for the booking flow to work without user authentication.
    match /appointments/{appointmentId} {
      allow read, write: if true;
    }
  }
}
